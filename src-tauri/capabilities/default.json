{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the FenceyMD window (events, window, app).",
  "windows": ["*"],
  "permissions": ["core:default"]
}
